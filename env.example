# Fanfiq — пример переменных окружения
# Скопируйте этот файл в .env (локально) или задайте переменные в Railway → Variables

############################################
# API / Workers (Backend)
############################################
# Полный URL PostgreSQL (пример для Railway)
# Формат: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=

# Redis для кэша/очередей (опционально)
# Пример: redis://default:PASS@HOST:PORT/0
REDIS_URL=

# Публичный домен API (для генерации абсолютных ссылок; без порта)
# Пример: https://api-production-xxxx.up.railway.app
EXTERNAL_API_URL=

# Включить отправку задач в Prefect (если используете Prefect Workers)
PREFECT_SUBMIT=1

# В контейнерах Railway полезно явно указать PYTHONPATH
PYTHONPATH=/app

# ВАЖНО: PORT на Railway устанавливается автоматически, НЕ задавайте его вручную
# Локально API слушает 58090 (см. Dockerfile: ${PORT:-58090})
# PORT=

############################################
# Frontend (Next.js)
############################################
# Публичный URL API, доступный из браузера (HTTPS, без порта)
# Пример: https://api-production-xxxx.up.railway.app
NEXT_PUBLIC_API_URL=

############################################
# Прочее (опционально)
############################################
# Время жизни кэша поиска (секунды)
SEARCH_CACHE_TTL=300
